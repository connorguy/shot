# Third-party notices

**Inter** by The Inter Project Authors, under the SIL Open Font License 1.1. It is bundled in the
Hardware type template (`templates/films/hardware-type/film/assets/fonts/inter.woff2`, license next to it
in `OFL.txt`) and embedded in the demo film on the site (`docs/demo.html`).

npm dependencies carry their own licenses in `node_modules`.
